"""Report-driven webpage/evidence screenshot capture for guideline 6-1
(오류페이지를 통한 정보 노출 여부).

Not importable as ``screenshot.modules.6-1`` (hyphenated directory name) —
load ``capture.py`` dynamically via ``importlib``, same convention used by
``diagnosis/registry.py`` for the sibling diagnosis modules. See ``run.py``
for a ready-made CLI entry point.
"""
